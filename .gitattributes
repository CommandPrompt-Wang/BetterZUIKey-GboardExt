# README.md 与 LSPosed 模块仓（BetterZUIKey-GboardExtModule）硬链接同一份文件，
# 固定为 LF，避免 core.autocrlf 在检出时改写字节、把硬链接弄成两份不同内容。
README.md text eol=lf

# 仓库内为 LF，但工作区会被 autocrlf 检出为 CRLF，导致 status 误报 modified。
PRINCIPLE.md text eol=lf
